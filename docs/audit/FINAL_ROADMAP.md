# FINAL_ROADMAP.md — план усунення за пріоритетами

Оцінка складності: XS < 1 год, S 1–4 год, M 1–2 дні, L 3–5 днів, XL > 1 тиждень.
Порядок є обов'язковим: кожен наступний блок спирається на попередній.

## Блок 0. Передумови (до будь-яких змін) — 1 день

| # | Дія | Складність |
| --- | --- | --- |
| 0.1 | Підняти окреме dev/staging-середовище з власною базою; заборонити тести на проді | M |
| 0.2 | Зробити і перевірити повний експорт бойових даних | S |
| 0.3 | Заморозити запуск повного імпорту keyCRM до завершення блоку 1 | XS |

## Блок 1. Безпека і права — 3–5 днів (блокуючий)

| # | Дія | Джерело | Складність |
| --- | --- | --- | --- |
| 1.1 | Звести дві моделі ролей до однієї: RLS-політики переписати на `has_permission()`, `app_role` звести до технічного `is_admin` | RLS_ROLE_MATRIX §1 | L |
| 1.2 | Тимчасово (до 1.1) синхронізувати `user_roles` з `user_access`, щоб система працювала не лише під адмінами | RLS_ROLE_MATRIX §4.1 | S |
| 1.3 | Закрити прайсові таблиці: закупівельні ціни й маржа — лише owner/finance; для продажників окрема view | SECURITY SEC-01 | M |
| 1.4 | Додати серверний guard ролі на всі операції, що використовують `admin()` (імпорт keyCRM, черга, маніфести, каталог, `approveEstimate`) | SECURITY SEC-03, BACKEND 2.5–2.6 | M |
| 1.5 | Rate limit на перевірку пароля власника | BACKEND 2.1 | S |
| 1.6 | Заборонити вебхук із `signature_mode='none'` без `endpoint_token`; закрити `worker` секретом | INTEGRATION §5 | S |
| 1.7 | Заповнювати `object_assignments` при призначенні бригади | DATABASE §2.7 | S |

## Блок 2. Цілісність даних — 4–6 днів (блокуючий)

| # | Дія | Складність |
| --- | --- | --- |
| 2.1 | Додати FK `estimates.client_id` / `object_id`, зв'язати 51 наявний запис, заборонити кошторис без об'єкта | L |
| 2.2 | Soft delete (`deleted_at`) на бізнес-сутностях; DELETE-політики → UPDATE | M |
| 2.3 | Оптимістичне блокування (`version`) на кошторисах, клієнтах, об'єктах, лідах | M |
| 2.4 | `price_book_version` у кошторисах + заповнення `price_history` тригером на `catalog_items` | M |
| 2.5 | Індекси на 46 FK + складені під RLS-фільтри (`owner_id`, `created_at`, `starts_at`) | S |
| 2.6 | Уніфікувати ідентичність користувача (`profiles.id` vs `profiles.user_id`) | S |

## Блок 3. Контур якості — 2–3 дні

| # | Дія | Складність |
| --- | --- | --- |
| 3.1 | Виправити 3 помилки типів TS2741 у переходах на калькулятори | XS |
| 3.2 | Прогнати `prettier --write`, звести лінт до нуля, увімкнути як gate | S |
| 3.3 | Unit-тести на 4 калькулятори з еталонними значеннями з Excel (мінімум 20 кейсів) | M |
| 3.4 | CI: build + typecheck + lint + tests на кожен push | M |

## Блок 4. Відповідність джерелу істини і фінансова точність — 2 дні

| # | Дія | Складність |
| --- | --- | --- |
| 4.1 | Застосувати коефіцієнт відходів 5% у покрівлі (зараз враховано лише нахльост 1.12) | S |
| 4.2 | Виводити в кошторисі джерело цін, дату актуальності та попередження про низьку маржу | S |
| 4.3 | CHECK-обмеження на невід'ємність сум, площ, кількостей; санітарні межі площі | S |
| 4.4 | Визначити канонічне сховище розрахунку в `estimates` (з 4 jsonb-полів) і нормалізувати рядки в `estimate_lines` | L |

## Блок 5. Продуктивність — 3–4 дні

| # | Дія | Складність |
| --- | --- | --- |
| 5.1 | Серверна пагінація для клієнтів, кошторисів, об'єктів, лідів (зараз `.limit(500)` приховує дані) | L |
| 5.2 | Динамічний `import()` для jspdf / html2canvas / xlsx; `manualChunks` (головний бандл 748 kB) | M |
| 5.3 | Віртуалізація великих таблиць і канбан-колонок | L |
| 5.4 | `staleTime` у QueryClient, усунення дублікатів query key | S |
| 5.5 | Субсет шрифтів PDF (1.23 MB) і стиснення footer PNG (1.1 MB) | S |

## Блок 6. Міграція keyCRM — 4–6 днів (після блоків 1–2)

| # | Дія | Складність |
| --- | --- | --- |
| 6.1 | Спроєктувати цільову таблицю замовлень і `applyOrder` | L |
| 6.2 | Додати сутність `tasks` у маніфест і `applyTask` → `crm_tasks` | M |
| 6.3 | Мапінг менеджерів keyCRM → користувачі ERP (зараз усе на одного власника) | M |
| 6.4 | Перенесення значень custom fields у поля ERP через `integration_field_mappings` | M |
| 6.5 | Прев'ю-валідація `default_owner_id` перед стартом; автоцикл `importChunk` у worker | S |
| 6.6 | Пробний повний імпорт на staging, звірка кількостей, лише потім прод | M |

## Блок 7. Відсутні модулі — окремі проєкти

Договори, акти, платежі/фінанси, документи з версіонуванням шаблонів і storage, склад, співробітники. Кожен — XL. Планувати після стабілізації блоків 1–3.

## Блок 8. Технічний борг

Прибрати або добудувати декларативний рушій напрямків (`directions`/`formulas`/`estimate_sections` + `src/lib/engines/*`), який дублює захардкоджені калькулятори; видалити мертві таблиці та `ModuleStub.tsx`.

## Мінімальний обсяг до промислового запуску

Блоки 0, 1, 2, 3 — приблизно **12–17 робочих днів**. Без них експлуатація системи більш ніж двома адміністраторами небезпечна, а імпорт keyCRM незворотний.
